#import "graphql/fragments/userCustomSets.graphql"

query buildList(
  $username: String!
  $first: Int!
  $after: String
  $filters: CustomSetFilters!
) {
  userByName(username: $username) {
    ...userCustomSets
  }
}
