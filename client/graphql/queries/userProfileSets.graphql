#import "graphql/fragments/userSets.graphql"

query userProfileSets(
  $username: String!
  $first: Int!
  $after: String
  $filters: CustomSetFilters!
) {
  userByName(username: $username) {
    username
    id
    ...userSets
  }
}
