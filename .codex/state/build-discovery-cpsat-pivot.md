# Build Discovery CP-SAT Pivot

Last updated: 2026-07-11

## Branch Strategy

- Active rewrite branch: `codex/build-discovery-cpsat-milestone2`
- Preserved beam/frontier branch: `archive/build-discovery-beam-frontier`
- Preserved beam/frontier tag: `archive/build-discovery-beam-frontier-20260711`
- Preserved CP-SAT spike branch: `codex/build-discovery-cpsat-experiment`
- Preserved CP-SAT spike tag: `archive/build-discovery-cpsat-spike-20260711`

Rollback to the old beam/frontier work:

```powershell
git switch archive/build-discovery-beam-frontier
```

Resume the CP-SAT rewrite:

```powershell
git switch codex/build-discovery-cpsat-milestone2
```

## Milestone 2 Target

The Notion PRD now defines Milestone 2 as the level-200 Iop full
single-element matrix:

- Class: Iop
- Level: 200
- Elements: Strength, Intelligence, Chance, Agility
- Budget tiers: 1-4
- AP targets: 7-12
- MP targets: 3-6
- Range targets: `None`, 0-6
- Total query rows: 3,072

## QA Assets To Keep

These assets remain useful even though the beam/package search algorithm is no
longer the product direction:

- Target-grid and matrix generation scripts.
- Matrix checker validation rules.
- AP/MP/Range inventory artifacts.
- Human/prod benchmark discovery tooling.
- Assumptions and progress logs.
- Multi-candidate artifact shape and diversity summaries.
- Expensive regression fixtures as comparison anchors, once they are adapted to
  CP-SAT output.

Active assumption review file:

- `.codex/state/build-discovery-cpsat-assumptions.md`

## Old Assets To Treat As Reference Only

- `server/oneoff/build_discovery_prototype.py`
- Beam/package search-specific seed, package, and pruning logic.
- Beam runtime measurements.
- Frontier artifacts generated by the beam solver.

These can still explain known good or bad builds, but they should not drive the
new product architecture.

## First CP-SAT Checkpoint

The first checkpoint imports the isolated CP-SAT experiment into the pivot
branch without wiring it into product code. This preserves the experiment and
gives reviewers a stable file to inspect before dependency packaging, model
hardening, and API integration begin.

Dependency note:

- OR-Tools is not added to `server/requirements.txt` in this checkpoint.
- The PRD still lists OR-Tools packaging/deploy footprint as an open risk.
- Production dependency management should be a separate reviewable diff.

## Query Adapter Checkpoint

The experiment now accepts query-shaped fields while staying isolated under
`server/oneoff/`:

- `--level`
- `--element`
- `--target-ap`
- `--target-mp`
- `--target-range`
- `--budget-tier`
- `--exo-policy`

It also exposes `solve_query(query, args)` so the existing matrix harness can
eventually call CP-SAT through the same generator seam used by beam artifacts.

Current limitations:

- Still a oneoff experiment, not GraphQL/product wiring.
- Locked items are not modeled yet.
- Avoided items are passed into candidate loading.
- `exoPolicy=none` disables generated exo variables.
- `exoPolicy=allow` and `exoPolicy=opti` are not yet distinct.
- OR-Tools dependency packaging is still unresolved.

## Matrix Harness Checkpoint

The level-diversity matrix runner now has an opt-in CP-SAT path:

```powershell
python server\scripts\build_discovery_level_diversity_matrix.py --solver cpsat ...
```

This keeps the old prototype/beam generator as the default while allowing small
CP-SAT Milestone 2 slices to be generated through the existing target,
validation, split-report, and Markdown artifact machinery.

The CP-SAT import is lazy so manifest generation and prototype matrix tests do
not require OR-Tools in environments where it has not been packaged yet.
